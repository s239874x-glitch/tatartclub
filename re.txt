<!DOCTYPE html>
<html>
<head>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>農工大美術部</title>

<style>
body{
margin:0;
height:100vh;
display:flex;
justify-content:center;
align-items:center;
font-family:sans-serif;
background:#0a6b37;
overflow:hidden;
}

/* 背景暗転 */
body::before{
content:"";
position:fixed;
top:0; left:0;
width:100%; height:100%;
background:rgba(0,0,0,0.3);
z-index:0;
pointer-events: none;
}

/* 🎁 */
#gift{
font-size:80px;
cursor:pointer;
z-index:1;
animation: float 2s infinite;
z-index:10;
}

#gift:active{
transform: scale(0.9);
}

@keyframes float {
  0% { transform: translateY(0); }
  50% { transform: translateY(-10px); }
  100% { transform: translateY(0); }
}

/* 中央固定ラッパー（重要） */
.box-wrapper{
position:absolute;
top:50%;
left:50%;
transform: translate(-50%, -50%);
z-index:1;
}

/* ボックス本体 */
.box{
background:white;
padding:40px;
border-radius:10px;
text-align:center;

/* 額縁 */
border: 12px solid #8b5a2b;
box-shadow:
  0 0 0 4px #d2b48c inset,
  0 10px 30px rgba(0,0,0,0.3);

/* 初期状態 */
transform: scale(0.5);
opacity:0;
pointer-events:none;
}

/* 出現 */
.box.active{
animation: pop 0.6s ease-out forwards;
pointer-events:auto;
}

/* ポップアウト（中央維持） */
@keyframes pop {
  0% {
    transform: scale(0.5);
    opacity:0;
  }
  70% {
    transform: scale(1.1);
    opacity:1;
  }
  100% {
    transform: scale(1);
    opacity:1;
  }
}

/* 揺れ（wrapperに適用） */
.box-wrapper.shake{
animation: shake 0.4s ease-in-out;
}

@keyframes shake {
  0% { transform: translate(-50%, -50%) translate(0,0); }
  20% { transform: translate(-50%, -50%) translate(-4px,2px); }
  40% { transform: translate(-50%, -50%) translate(4px,-2px); }
  60% { transform: translate(-50%, -50%) translate(-3px,1px); }
  80% { transform: translate(-50%, -50%) translate(3px,-1px); }
  100% { transform: translate(-50%, -50%) translate(0,0); }
}

/* テキスト */
h1{
font-size:2.5em;
margin:0 0 20px 0;
}

p{
font-size:1.3em;
margin:10px 0;
}

.small{
font-size:0.7em;
color:gray;
}

</style>
</head>

<body>

<!-- 🎁 -->
<div id="gift">🎁</div>

<!-- 中央固定 -->
<div class="box-wrapper">
  <div class="box">

    <h1>おめでとう！</h1>

    <p>
    あなたは農工大の美術部を見つけました！<br>
    <a href="https://x.com/nokonokoart" target="_blank">ツイッター</a>、  
    <a href="https://www.instagram.com/artclub_tuat/" target="_blank">インスタグラム</a>もあります！<br>
    </p>

    <p class="small">
    ※ネタバラシはこっそりおねがいします 🤫
    </p>

  </div>
</div>

<!-- 音 -->
<audio id="se" src="sound.mp3" preload="auto"></audio>

<script>
document.getElementById("gift").addEventListener("click", function(){

  // 🎁消す
  this.style.display = "none";

  const box = document.querySelector(".box");
  const wrapper = document.querySelector(".box-wrapper");

  // 少し間を作る
  setTimeout(()=>{
    box.classList.add("active");
  }, 100);

  // 揺れ
  setTimeout(()=>{
    wrapper.classList.add("shake");
  }, 600);

  // 音
  const audio = document.getElementById("se");
  if(audio){
    audio.currentTime = 0;
    audio.play().catch(()=>{});
  }

  // バイブ
  if(navigator.vibrate){
    navigator.vibrate([100,50,100]);
  }

});
</script>

</body>
</html>
